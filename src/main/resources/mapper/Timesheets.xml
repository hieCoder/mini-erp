<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shsoftvina.erpshsoftvina.mapper.TimesheetsMapper">

    <select id="findAll" parameterType="com.shsoftvina.erpshsoftvina.entity.Timesheets" resultMap="timesheetsResultMap">
        SELECT t.*, u.FULLNAME
        FROM TIMESHEETS t
        INNER JOIN USER u ON t.USER_ID = u.id
        LIMIT #{start}, #{pageSize}
    </select>

    <select id="totalWorkingDateByYear" parameterType="String" resultType="java.util.Map">
        SELECT YEAR(WORK_DATE) AS year, COUNT(*) AS workdays
        FROM TIMESHEETS
        WHERE USER_ID = #{userID}
        GROUP BY year
        ORDER BY year ASC;
    </select>

    <select id="totalWorkingDateByMonth" parameterType="String" resultType="java.util.Map">
        SELECT MONTH(WORK_DATE) AS month, COUNT(*) AS workdays
        FROM TIMESHEETS
        WHERE USER_ID = #{userID} AND YEAR(WORK_DATE) = #{year}
        GROUP BY month
        ORDER BY month ASC;
    </select>

    <resultMap id="timesheetsResultMap" type="com.shsoftvina.erpshsoftvina.entity.Timesheets">
        <id property="id" column="ID"/>
        <result property="checkin" column="CHECKIN"/>
        <result property="checkout" column="CHECKOUT"/>
        <result property="ot" column="ot"/>
        <result property="workDate" column="WORK_DATE"/>
        <association property="user" column="USER_ID" javaType="com.shsoftvina.erpshsoftvina.entity.User" resultMap="userResultMap">
        </association>
    </resultMap>

    <resultMap id="userResultMap" type="com.shsoftvina.erpshsoftvina.entity.User">
        <id property="id" column="id"/>
        <result property="fullname" column="FULLNAME"/>
    </resultMap>

</mapper>