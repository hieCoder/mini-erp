<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shsoftvina.erpshsoftvina.mapper.TaskMapper">

    <select id="findAll" resultMap="taskResultMap">
        SELECT t.*, u.fullname
        FROM task t
        INNER JOIN user u ON t.user_id = u.id
        WHERE t.status = #{status}
        AND (
                u.fullname LIKE CONCAT('%', #{search}, '%') OR
                t.content LIKE CONCAT('%#', #{search}, '%') OR
                t.title LIKE CONCAT('%', #{search}, '%')
        )
        LIMIT #{start}, #{pageSize}
    </select>

    <resultMap id="taskResultMap" type="com.shsoftvina.erpshsoftvina.entity.Task">
        <id property="id" column="id"/>
        <result property="statusTask" column="status_task"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdDate" column="created_date"/>
        <result property="startDate" column="start_date"/>
        <result property="closeDate" column="close_date"/>
        <result property="progress" column="progress"/>
        <result property="priority" column="priority"/>
        <result property="status" column="status"/>
        <association property="user" column="user_id" javaType="com.shsoftvina.erpshsoftvina.entity.User" resultMap="userResultMap">
        </association>
    </resultMap>

    <resultMap id="userResultMap" type="com.shsoftvina.erpshsoftvina.entity.User">
        <id property="id" column="id"/>
        <result property="fullname" column="fullname"/>
    </resultMap>

    <insert id="registerTask" parameterType="com.shsoftvina.erpshsoftvina.entity.Task">
        INSERT
        INTO task (id, status_task, title, content, user_id, created_date, close_date, priority, progress, status)
        VALUES (#{id}, #{statusTask}, #{title}, #{content}, #{user.id}, #{createdDate}, #{closeDate}, #{priority}, #{progress}, #{status});
    </insert>

</mapper>